# Examples

The primary example is the checked-in local fixture used by `evaluate.py`.
It demonstrates prompt-injection defense, untrusted-context isolation, sensitive-data controls, output inspection, and bounded tool authorization without requiring external accounts or
credentials. Add new examples with expected outcomes and a short explanation.
